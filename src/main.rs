use super_prime_mover::*;

fn main() {

    let board = Board::default();
    // TODO: Populate board

    let game = Game::with(board);

    loop {

    }
}

fn print_board(board: &Board) {
    for tile in board.tiles {

    }
}